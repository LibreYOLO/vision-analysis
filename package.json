{
  "name": "vision-analysis",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix website",
    "test": "python3 scripts/test_verified_results_compat.py && python3 -m unittest discover -s scripts -p \"test_submission_credits.py\" && python3 -m unittest discover -s scripts -p \"test_benchmark_compat.py\" && python3 -m unittest discover -s scripts -p \"test_g0_g1_catalog.py\"",
    "vercel-build": "npm run build --prefix website"
  }
}
